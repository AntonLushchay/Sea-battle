{
	// Наследует все настройки из базового конфигурационного файла.
	"extends": "../../tsconfig.base.json",

	// Указывает, что в данный проект входят только файлы из директории "src".
	"include": ["src"],

	"compilerOptions": {
		// Явно указываем библиотеку, чтобы TypeScript НЕ подставлял "DOM" по умолчанию.
		// Гарантирует, что код в этом пакете будет универсальным и не будет зависеть от среды выполнения.
		"lib": ["ES2022"],

		// Обязательная опция для проектов, на которые ссылаются через "references". Ускоряет пересборку.
		"composite": true,

		// Указывает компилятору создавать файлы объявлений типов (.d.ts) для этого проекта.
		"declaration": true,

		// Говорит компилятору создавать ТОЛЬКО файлы объявлений (.d.ts), но не JavaScript файлы.
		// Это нужно, т.к. 'shared' пакет используется другими пакетами на этапе разработки, а не как исполняемый код.
		"emitDeclarationOnly": true,

		// Указывает, куда складывать сгенерированные файлы объявлений.
		"outDir": "dist/types",

		// Эта опция здесь может показаться противоречивой с `emitDeclarationOnly`, но она нужна, чтобы `emitDeclarationOnly` работало.
		// `noEmit: true` полностью отключает любую генерацию файлов. `noEmit: false` позволяет `emitDeclarationOnly` делать свое дело.
		"noEmit": false
	}
}
